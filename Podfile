# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyReceipes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

   pod 'SwiftyJSON'
   pod 'Kingfisher', '~> 7.0'

  # Pods for MyReceipes

  target 'MyReceipesTests' do
    inherit! :search_paths

    # Pods for testing
  end

end
